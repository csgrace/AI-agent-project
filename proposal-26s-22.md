# Part I. Preliminary Requirement Analysis

## 1. Functional Requirements

系统共包含 **5 个正交的核心功能模块**，每个模块代表一个独立的用户价值单元：

### 1.1 智能任务规划助手

1. 自动读取多源信息（校历、Blackboard 截止日期、个人待办事项）
2. 调用 LLM 进行综合分析，生成整体任务规划建议
3. 支持用户对规划结果进行确认、调整和重新生成

### 1.2 校园知识库问答系统

1. 检索并索引学校发布的各类官方文件（政策文档、课程大纲、专业培养方案、通知公告等）
2. 基于 RAG（检索增强生成）技术，根据用户提问从知识库中提取相关内容并生成准确回答
3. 支持知识库的定期自动更新

### 1.3 文档处理与学习辅助 Skill

1. 支持 PPT、PDF、Markdown 等格式文档的上传与内容解析
2. 提供两个可调用的 Skill：
   - **内容摘要**：根据文档内容生成结构化摘要
   - **测验题生成**：基于文档内容自动生成测验题目（含答案）
3. Skill 结果可供用户保存或进一步编辑

### 1.4 自动化脚本生成与执行

1. Agent 根据用户自然语言需求自动生成可执行的自动化脚本（如批量文件处理、数据抓取等）
2. 支持脚本的保存、编辑和复用

### 1.5 个性化选课推荐引擎

1. 综合学生的专业要求、已修课程、兴趣偏好和职业规划，并结合学校的选课规则、课程先修关系等政策数据
2. 智能推荐下学期的选课方案，并解释推荐理由

---

## 2. Non-functional Requirements

### 2.1 安全性与合规性

(1) **数据隐私**：对用户的个人数据（如课程表、成绩、待办事项、API key）进行加密存储和传输。

(2) **内容安全**：对智能体生成的内容进行由用户批准的安全审查（选课内容、日程安排等）以及沙箱隔离（写的自动化脚本），避免有害或不恰当的内容对用户造成损害。

### 2.2 可扩展性与可维护性

(1) **功能扩展**：系统架构应支持未来新增功能，例如接入新的校园系统、支持更多类型的文档格式或增加新的 Skill。

(2) **知识库更新**：应配置skill在调用知识库时检查时效性，及时更新和维护学校官方文件知识库。

---

## 3. Technical Requirements

### 3.1 模型与 AI 技术

(1) 使用 LangChain 框架构建智能体应用的主干部分，包括工作流，管理其“思考”、调用工具和执行任务的流程。具体而言，对于功能需求里的校园知识库问答系统、个性化选课推荐引擎，由于其可能涉及大量上下文交互，因此将其分离为单独的agent，在前台指令agent进行相关任务时，使用langchain的多智能体系统中的hands-off模式将控制权移交给对应的智能体。前端采用React架构。

### 3.2 工具与系统集成

(1) 集成 PPT、PDF、Markdown 等格式的解析库，以便提取内容生成摘要或测验题。

(2) 将实现的各类 API 封装为 MCP 格式，统一供模型调用。

(3) 创建沙箱环境以安全地执行用户创建的自动化脚本。

---

## 4. Data Requirements

### 4.1 知识库数据

| 属性 | 说明 |
|------|------|
| **来源** | 学校官方发布的各类文件，如政策文档、课程大纲、专业培养方案、通知公告等 |
| **用途** | 用于构建 RAG 系统的知识库，确保智能体能准确回答基于这些官方文件的问题 |
| **获取方式** | 设置定时运行任务，调用 MCP（如 fetch）从特定 URL 定期爬取 |

### 4.2 用户上下文数据

| 属性 | 说明 |
|------|------|
| **来源** | 用户的个人资料、已修课程记录、兴趣偏好、对话历史 |
| **用途** | 为用户提供个性化的服务，如生成符合其专业要求和兴趣的选课方案，或根据其历史习惯优化任务规划 |
| **获取方式** | 当前对话上下文以及长期记忆 |

### 4.3 操作数据

| 属性 | 说明 |
|------|------|
| **来源** | 校历系统、Blackboard (BB) 平台、个人待办应用、用户创建的自动化脚本 |
| **用途** | 这是智能体执行任务的基础，例如读取截止日期来规划任务，或读取课程表来推荐选课 |
| **获取方式** | 实现一个 MCP 服务器，从指定 URL 处获取 |

### 4.4 策略与规则数据

| 属性 | 说明 |
|------|------|
| **来源** | 学校的选课规则、毕业要求、课程先修关系、自动化脚本的编写规范 |
| **用途** | 约束智能体的行为，确保其推荐的选课方案符合学校规定，生成的脚本安全合规 |
| **获取方式** | 以配置文件或规则库形式管理，支持人工更新 |
