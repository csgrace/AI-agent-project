# Design Report -- Sprint 1

26s- 22组【选题三】

## 1. 建筑设计

### 1.1 架构总览

本系统采用前后端分离的现代Web应用架构。前端基于React+Vite+TailwindCSS构建单页面应用（SPA），后端基于Python+LangChain构建智能体服务。前后端通过RESTful API及Server-Sent Events（SSE）进行通信，实现数据的动态展示、流式响应和用户操作的实时反馈。

### 1.2 架构图

<img width="1993" height="1220" alt="image" src="https://github.com/user-attachments/assets/b21d6df6-4ae6-4278-9deb-78f1acda8ef2" />


### 1.3. 前端架构详细描述

前端采用React+TypeScript+Vite技术栈，主要包含以下模块：

#### 1.3.1 核心组件

- **App容器**：应用根节点，统一挂载主题（深色/浅色）、全局样式和状态管理Provider。
- **路由（Router）**：基于react-router-dom，控制页面逻辑入口，根据URL切换对应的页面组件（首页、登录页、数据展示页、管理页等）。
- **ModernCalendar**：核心日历组件，负责展示和管理日程事件，支持月份导航、事件显示及添加事件入口。
- **StatusBar**：状态栏组件，位于页面顶部，显示应用标题、深色模式切换按钮及个人中心按钮。
- **EventForm**：事件表单模态框组件，用于创建和编辑日历事件，包含标题、时间、描述、优先级等字段。
- **EventDetail**：事件详情模态框组件，展示事件的完整信息，并提供编辑和删除操作入口。
- **业务视图组件**：可复用的界面小组件，如聊天界面、问答结果展示、引用管理面板、文档上传/预览组件等。

#### 1.3.2 功能模块

- **日程规划模块**：集成ModernCalendar，EventForm，EventDetail等组件，提供日历管理、事件创建/编辑/删除、事件详情查看等功能。
- **校园信息助手模块**：提供聊天界面、问答结果展示和引用管理。支持流式显示助手回复，并展示回答的置信度及引用来源（如PDF文档）。
- **文档处理模块**：支持用户上传文档，预览文档内容，并对已索引的文档进行查询。
- **其他模块**：包含帮助、设置等辅助功能页面。

#### 1.3.3 数据流与状态管理

- **API请求层**：统一使用RESTful风格管理所有与后端的数据交互，集中处理请求、异常、Token鉴权。
- **本地存储**：使用Cookie或LocalStorage持久化用户Token、主题偏好等非敏感信息。
- **流式数据接收**：通过EventSource或Fetch API接收后端SSE流，实时解析并更新界面（如智能助手的逐字回复、工具调用状态）。

### 1.4 后端架构详细描述

后端基于Python + LangChain构建，主要包含以下模块：

#### 1.4.1 智能体核心

- **AgentFactory**：负责构建和配置智能体，注入系统提示词（包含当前时间戳）、技能目录及工具集。
- **AgentRunner**：管理智能体的执行和事件流，处理流式输出、中间件调用及异常捕获。
- **中间件**：包括摘要中间件（处理长对话上下文）和错误处理中间件（捕获工具异常，保证智能体稳定运行）。

#### 1.4.2 技能系统

- **技能注册服务**：扫描、解析并注册基于文件定义的技能，维护技能注册表。
- **技能加载工具**：根据智能体的需求动态加载和执行特定技能。
- **技能执行**：技能内部可调用一个或多个工具完成复杂任务。

#### 1.4.3 工具系统

- **日历工具**：提供日历事件的查询、添加、更新、删除等原子操作。
- **技能加载工具**：允许智能体在运行时加载和执行已注册的技能。
- **对象存储工具**：管理跨工具共享的大对象（如文档内容、中间结果）。
- **其他辅助工具**：如事件过滤、持续时间估计等。

#### 1.4.4 核心服务

- **日历服务**：管理主日历和草稿日历，处理事件的审批流程和同步逻辑。
- **对象存储**：提供文档、中间数据等对象的存储和检索功能。
- **初始化服务**：负责系统启动时的配置加载、环境检查及资源初始化。

## 2. 用户界面设计

### 2.1 整体布局设计

#### 2.1.1 状态栏

- **位置**：页面顶部，固定显示
- **内容**：左侧：应用标题“智能校园助手”，右侧：深色模式切换按钮和个人中心按钮
- **样式**：白色背景（深色模式下为深灰色），底部有细微阴影，提供清晰的视觉分隔

#### 2.1.2 侧边栏

- **位置**：页面左侧，固定宽度
- **内容**：功能选项区域：包含“日程规划”和“校园信息助手”两个主要功能按钮；其他区域：包含“帮助”和“设置”链接
- **样式**：白色背景（深色模式下为深灰色），右侧有边框分隔，选中状态使用绿色背景和文字，提供清晰的视觉反馈

#### 2.1.3 主内容区域

- **位置**：侧边栏右侧，占据剩余空间
- **布局**：根据选择的功能显示不同内容，支持响应式设计

### 2.2 日程规划界面

<img width="1084" height="620" alt="image (1)" src="https://github.com/user-attachments/assets/d0e244ae-2ae0-4824-b8a3-18dcd77b968c" />

<img width="1309" height="761" alt="image (2)" src="https://github.com/user-attachments/assets/59723ad6-dc98-40c4-aca4-ca8aa06d4b01" />


#### 2.2.1 日历组件

- **位置**：主内容区域左侧（占据2/3宽度）
- **功能**：
  - 月份导航：支持切换月份和返回本月
  - 事件显示：根据日期显示当天的事件，不同颜色标签的事件使用不同背景色
  - 添加事件：提供“添加事件”按钮
- **样式**：白色卡片背景（深色模式下为深灰色），圆角设计，内部使用网格布局显示日历，事件使用彩色标签区分

#### 2.2.2 事件表单模态框

- **位置**：页面中央，覆盖在原页面之上
- **功能**：
  - 必填选项：标题、起始时间、结束时间（起始时间默认为当前时间）
  - 更多选项：描述、地点、优先级、状态、颜色标签、标签
  - 提交和取消按钮
- **样式**：白色卡片背景（深色模式下为深灰色），圆角设计，阴影效果，背景使用模糊效果增强层次感

#### 2.2.3 事件详情模态框

- **位置**：页面中央，覆盖在原页面之上
- **功能**：显示事件的详细信息，提供编辑和删除按钮
- **样式**：白色卡片背景（深色模式下为深灰色），圆角设计，阴影效果，背景使用模糊效果增强层次感

#### 2.2.4 智能助手对话框

- **位置**：主内容区域右侧（占据1/3宽度）
- **功能**：
  - 空状态：显示智能助手介绍，包含图标、标题和描述
  - 聊天界面：显示用户和助手的对话
  - 输入区域：支持多行输入，发送按钮根据输入状态变化颜色
- **样式**：白色卡片背景（深色模式下为深灰色），圆角设计，输入区域与聊天区域共享边框

### 2.3 校园信息助手界面

<img width="1190" height="674" alt="image (3)" src="https://github.com/user-attachments/assets/f2194ad9-0b6f-431f-8d74-561fb3eb50a4" />

<img width="1051" height="514" alt="image (4)" src="https://github.com/user-attachments/assets/0a8893b5-5285-48b9-b05a-f614520bd6c4" />


#### 2.3.1 聊天界面

- **位置**：主内容区域中央
- **功能**：
  - 空状态：显示智能助手介绍
  - 聊天记录：显示用户和助手的对话
  - 输入区域：支持多行输入，发送按钮根据输入状态变化颜色
- **样式**：白色卡片背景（深色模式下为深灰色），圆角设计，输入区域与聊天区域共享边框

#### 2.3.2 回答来源与置信度展示界面

- **位置**：主内容区域右侧（占据1/3宽度）
- **功能**：
  - 空状态：显示“暂无引用”
  - 信息展示：展示回答的置信度评分，以及引用来源的文件列表。若当前回答未基于后端文件来源生成，则显示“通用大模型正在为您回答，未使用PDF/文档作为直接引用来源。”
  - 交互与详情：点击引用来源文件后，可打开并查看该文档。右侧同时展示置信度评分以及文档中与回答相关的匹配关键词。
- **样式**：白色卡片背景（深色模式下为深灰色），圆角设计

### 2.4 交互设计

#### 2.4.1 深色模式

- **切换方式**：通过状态栏中的切换按钮
- **效果**：所有界面元素的颜色会平滑过渡到深色模式，包括背景、文字、边框等
- **实现**：使用 Tailwind CSS 的深色模式支持，通过类名切换实现

#### 2.4.2 事件操作

- **添加事件**：点击“添加事件”按钮打开表单
- **编辑事件**：点击事件详情中的“编辑”按钮打开表单
- **删除事件**：点击事件详情中的“删除”按钮
- **查看事件**：点击日历上的事件打开详情

#### 2.4.3 智能助手交互

- **发送消息**：在输入框中输入内容，点击发送按钮或按 Enter 键
- **输入反馈**：发送按钮在有输入时为绿色，无输入时为浅绿色
- **响应显示**：助手的响应会实时显示，支持流式输入效果

### 2.5 响应式设计

- **大屏幕**：侧边栏固定显示，主内容区域使用网格布局
- **中等屏幕**：保持基本布局，但调整元素大小
- **小屏幕**：侧边栏可折叠，主内容区域垂直堆叠

### 2.6 后端消息

- tool 内部的 print 内容
- 流式传输的 thought 和特定输出内容
- commit 的交互内容
